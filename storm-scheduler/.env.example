# Storm Scheduler Environment Configuration
# Copy this file to .env and adjust the values for your environment

# Required: Path to your Apache Storm installation
STORM_HOME="/usr/local/storm"

# Optional: Path to your ZooKeeper installation (if not using system ZooKeeper)
# ZK_HOME=/usr/local/zookeeper

# Optional: Custom log directory (default: PROJECT_ROOT/logs)
# Useful if you encounter permission issues with the default logs directory
# LOG_DIR=/tmp/storm-logs
# LOG_DIR=/home/your-username/storm-logs

